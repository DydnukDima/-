імпортувати сокет 
імпорт _thread


основна характеристика():
	host = '0.0.0.0'
	порт = 9999
	користувачів = 4

	s = сокет . роз'єм()
	s. ((вузол, порт)))
	s. слухати(користувачі)
	print("Прослуховування підключень")

	кони = []
	підключення = False 

	def connUsers():
		для користувачів у межах досяжності(користувачі):
			conn, addr = s. прийняти()
			конс. додавання(конн)
			підключення = True
			print(додавання , 'підключений')
			для кожного в  діапазоні(len(конуси)):
				_thread. start_new_thread(sendData, (кожен, ))
			підключення = False 
			_thread. start_new_thread(ConnUsers, ())

	def надіслатидані(користувач):
		тоді як true:
			data = conns[користувач]. recv(1024). декодування('utf-8')
			для кожного в  діапазоні(len(конуси)):
				conns[кожен]. ((дані). кодування('utf-8'))
			якщо не дані або підключення :
				Перерва
		print("Закриття підключень")
		s. закрити()


	_thread. start_new_thread(ConnUsers, ())

	тоді як true:
		Проходять


якщо __name__ == "__main__":
	Основні()
